module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // 'postcss-at-rules-variables': {}, // Optional, helps with @rules
    // 'postcss-discard-duplicates': {}, // Optional
  },
}
